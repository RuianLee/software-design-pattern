# 版本對照說明

本專案提供兩個版本的實作，用於展現設計模式的重要性。

---

## 📁 專案結構

```
道館四/
├── src/                    # 原始開發目錄
├── 初版實作/                # 違反 OCP 的設計（反例）
├── 進階實作/                # 符合 OCP 的設計（正例）- 待實作
├── bin/                    # 編譯輸出目錄
├── test/                   # 測試資料
└── resources/              # 資源檔案
```

---

## 🔴 初版實作（反例）

### 設計特點
- **違反開放封閉原則 (OCP)**
- 所有牌型比較邏輯集中在 `CardPattern` 類別
- 子類只做單純繼承，不覆寫方法

### 類別結構
```
CardPattern (普通類別)
├── SingleCard (單純繼承)
├── Pair (單純繼承)
├── Straight (單純繼承)
└── FullHouse (單純繼承)
```

### 核心問題
`CardPattern.java` 的 `isGreaterThan()` 方法：
```java
public boolean isGreaterThan(CardPattern other) {
    // ... 基本檢查 ...

    if (patternName.equals("單張")) { /* 單張邏輯 */ }
    if (patternName.equals("對子")) { /* 對子邏輯 */ }
    if (patternName.equals("順子")) { /* 順子邏輯 */ }
    if (patternName.equals("葫蘆")) { /* 葫蘆邏輯 */ }

    return false;
}
```

### 缺點
1. ❌ 新增牌型時必須修改 `CardPattern` 類別（違反 OCP）
2. ❌ 所有邏輯擠在一個方法中，難以維護
3. ❌ 使用字串比對判斷牌型，容易出錯
4. ❌ 無法讓子類自訂比較邏輯

### 編譯與執行
```bash
cd 初版實作
javac -d ../bin *.java
cd ..
java -cp bin Game < test/input.txt
```

---

## 🟢 進階實作（正例）- 待實作

### 設計目標
- **符合開放封閉原則 (OCP)**
- 使用抽象方法讓子類各自實作比較邏輯
- 新增牌型時只需新增子類，不修改現有程式碼

### 類別結構（規劃）
```
CardPattern (抽象類別)
├── abstract isGreaterThan()
│
├── SingleCard (覆寫 isGreaterThan)
├── Pair (覆寫 isGreaterThan)
├── Straight (覆寫 isGreaterThan)
└── FullHouse (覆寫 isGreaterThan)
```

### 優點（預期）
1. ✅ 新增牌型時只需新增子類（符合 OCP）
2. ✅ 每個牌型的邏輯獨立，易於維護
3. ✅ 利用多型，程式碼更簡潔
4. ✅ 子類可自訂比較邏輯

### 編譯與執行
```bash
cd 進階實作
javac -d ../bin *.java
cd ..
java -cp bin Game < test/input.txt
```

---

## 📊 對照比較

| 項目 | 初版實作 | 進階實作 |
|------|---------|---------|
| **設計原則** | 違反 OCP | 符合 OCP |
| **CardPattern** | 普通類別 | 抽象類別 |
| **isGreaterThan** | 集中判斷 | 抽象方法 |
| **子類實作** | 單純繼承 | 覆寫方法 |
| **新增牌型** | 需修改父類 | 只需新增子類 |
| **維護性** | 低 | 高 |
| **擴展性** | 低 | 高 |

---

## 🎯 學習重點

1. **開放封閉原則 (OCP)**
   - 對擴展開放：可以新增新的牌型
   - 對修改封閉：不需修改現有程式碼

2. **多型的力量**
   - 透過抽象方法強制子類實作
   - 利用繼承和覆寫達成程式碼重用

3. **設計模式的重要性**
   - 良好的設計讓程式更容易維護和擴展
   - 反例可以幫助理解為什麼需要設計模式

---

## 📝 下一步

1. 在「進階實作」目錄中實作符合 OCP 的設計
2. 比較兩個版本的差異
3. 思考如果要新增「同花順」牌型，兩個版本分別需要做哪些修改
